# Docker Commands - may-mobile

## Build
docker build -t ozanemrahyakupoglu/may-mobile:1.0.2 .

## Push (Docker Hub)
docker push ozanemrahyakupoglu/may-mobile:1.0.2

## Run
docker run -d \
  --name may-mobile \
  -p 8092:8092 \
  ozanemrahyakupoglu/may-mobile:1.0.2

---

# Docker Compose Commands - may-mobile

## Build
docker compose build

## Build (with version)
VERSION=1.0.2 docker compose build

## Push (Docker Hub)
docker compose push

## Push (with version)
VERSION=1.0.2 docker compose push

## Run
docker compose up -d

## Run (with version)
VERSION=1.0.2 docker compose up -d

# Docker Commands - may-mobile

## Build
docker build -t ozanemrahyakupoglu/may-mobile:1.0.3 .

## Push (Docker Hub)
docker push ozanemrahyakupoglu/may-mobile:1.0.3

## Run
docker run -d \
  --name may-mobile \
  -p 8092:8092 \
  ozanemrahyakupoglu/may-mobile:1.0.3

---

# Docker Compose Commands - may-mobile

## Build
docker compose build

## Build (with version)
VERSION=1.0.3 docker compose build

## Push (Docker Hub)
docker compose push

## Push (with version)
VERSION=1.0.3 docker compose push

## Run
docker compose up -d

## Run (with version)
VERSION=1.0.3 docker compose up -d
